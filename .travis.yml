language: node_js
os: linux
node_js:
- lts/*
env:
  - LIGHTHOUSE_API_KEY=adf615154a8d49588da7f64376c841d0
before_install:
  - yarn install -g npm@latest
  - yarn install -g greenkeeper-lockfile@1
install:
  - yarn install
before_script:
  - greenkeeper-lockfile-update
script:
  - npm run lint
after_script:
  - greenkeeper-lockfile-upload
after_success:
  - yarn lh -- --perf=90 --pwa=100 --no-comment https://
prove_v2.io.netlify.com/
branches:
  only:
    - master
    - /^greenkeeper.*$/
